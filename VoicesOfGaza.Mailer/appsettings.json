{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Still have to figure out why removing the asterisks break the Lambda function.
  // For now we need both in PROD. When fixing this, also check AppConfiguration file
  "AllowedHosts": "*;voicesofgaza.org.za"
}
